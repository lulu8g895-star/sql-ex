/* use a SQL query to group your orders by year and count the tota */

SELECT 
    YEAR(order_date) AS order_year, 
    COUNT(order_id) AS total_orders
FROM 
    orders
GROUP BY 
    YEAR(order_date)
ORDER BY 
    order_year DESC
